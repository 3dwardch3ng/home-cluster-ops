apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: namespaces
  namespace: flux-system
spec:
  interval: 1h
  path: ./kubernetes/infrastructure/namespaces/ns
  prune: true
  sourceRef:
    kind: GitRepository
    namespace: flux-system
    name: flux-system