apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: clusterissuers
  namespace: flux-system
spec:
  suspend: false
  interval: 1h
  targetNamespace: cert-manager
  path: ./kubernetes/apps/certissuers
  prune: true
  sourceRef:
    kind: GitRepository
    namespace: flux-system
    name: flux-system
  dependsOn:
    - name: clusterissuer-secrets
  postBuild:
    substituteFrom:
      - kind: Secret
        name: clusterissuer-secrets