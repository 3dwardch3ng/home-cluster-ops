apiVersion: cilium.io/v2
kind: CiliumClusterwideNetworkPolicy
metadata:
  name: ingress-nodes
spec:
  endpointSelector:
    matchLabels:
      rpi5.cluster.policy/ingress-nodes: "true"
  ingress:
    - fromEntities:
        - host
        - remote-node
