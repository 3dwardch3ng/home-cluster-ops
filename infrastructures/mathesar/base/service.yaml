apiVersion: v1
kind: Service
metadata:
  name: mathesar
  namespace: mathesar
  labels:
    app.kubernetes.io/name: mathesar
spec:
  selector:
    app.kubernetes.io/name: mathesar
  type: ClusterIP
  internalTrafficPolicy: Cluster
  ports:
    - protocol: TCP
      port: 18000
      targetPort: 8000
      name: web
