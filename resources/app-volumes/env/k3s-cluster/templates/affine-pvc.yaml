apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: affine-pvc
  namespace: affine
  labels:
    name: affine-pvc
spec:
  storageClassName: local-path
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
