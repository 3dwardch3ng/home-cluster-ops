apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: anything-llm-pvc
  namespace: anything-llm
  labels:
    name: anything-llm-pvc
spec:
  storageClassName: local-path
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
