apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: worldpress-pvc
  namespace: worldpress
  labels:
    name: worldpress-pvc
spec:
  storageClassName: local-path
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 64Gi